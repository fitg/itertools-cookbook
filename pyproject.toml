[tool.poetry]
name = "itertools-cookbook"
version = "0.1.0"
description = "A small project to demonstrate Python's itertools module."
authors = ["Gustaw Fit <gustaw.fit@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
click = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
